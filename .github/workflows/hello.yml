name: helloWorld
on: push
jobs: 
    hello:
        runs-on: ubuntu-latest
        env:
         ACTOR: ${{ github.run.id }}
        steps:
            - run: echo "${ACTOR}"
            - uses: actions/checkout@v4
            