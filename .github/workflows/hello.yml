name: helloWorld
on: push
jobs: 
    hello:
        runs-on: ubuntu-latest
        env:
         ACTOR: ${{ format('nozaki,ntama.','Hiworld')}}
        steps:
            - run: echo "${ACTOR}" 
            - uses: actions/checkout@v4
            