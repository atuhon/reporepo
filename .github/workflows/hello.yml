name: helloWorld
on: push
jobs: 
    hello:
        runs-on: ubuntu-latest
        env:
         ACTOR: ${{ contains('Hello','el') }}
         EVENT: ${{ github.event.*.html_url}}
        steps:
            - run: echo "${ACTOR}" "${EVENT}" 
            - uses: actions/checkout@v4
            