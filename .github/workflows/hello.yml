name: helloWorld
on: push
jobs: 
    hello:
        runs-on: ubuntu-latest
        env:
         ACTOR: ${{ github.event.issue.labels.*.name }}

        steps:
            - run: echo "${ACTOR}" 
            - uses: actions/checkout@v4
            