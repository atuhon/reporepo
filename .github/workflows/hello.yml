name: helloWorld
on: push
jobs: 
    hello:
        runs-on: ubuntu-latest
        env:
         ACTOR: ${{ contains('Hello','el') }}
        steps:
            - run: echo "${ACTOR}"
            - uses: actions/checkout@v4
            